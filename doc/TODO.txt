================================================================================
TODO — BS & DEMENTIA PROJECT (doc/)
================================================================================
Last updated: 2026-05-14

Items removed when resolved. See git log for history.
All CRITICALs and most CONFIRMs are resolved — pipeline is ready for first run.


================================================================================
CONFIRM — VERIFY BEFORE TRUSTING RESULTS
================================================================================

[CONFIRM-4] DBSO underreporting 2010–2012 — check after first run
  DBSO became mandatory in 2010; registration completeness may have been lower
  in 2010–2012, affecting the E66 obesity comparator pool.
  Action:   After running main_build_cohorts(), examine match rate warnings.
            If zero-match rates are high for 2010–2012, add sensitivity analysis
            restricting to index dates from 2013 onwards.
            Contact DBSO registry managers for documentation on completeness
            before citing specific years in the manuscript.
  See:      study1_methods_plan.md section 11 [A1] and [A4].

[CONFIRM-5] GLP-1 exclusion in obesity comparator — confirm with co-authors
  Tentative plan: include GLP-1 users in main analysis with GLP-1 as covariate;
  add sensitivity analysis excluding GLP-1 users at obesity-range dose
  (liraglutide ≥ 2.4 mg, semaglutide ≥ 1 mg; ATC A10BJ).
  Action:   Confirm analytical decision with co-authors before implementing.
  See:      study1_methods_plan.md section 11 [B5].


================================================================================
AWAIT SUPERVISOR MEETING — DO NOT CODE UNTIL RESOLVED
================================================================================

[SUPERVISOR-1] Additional confounder flags — confirm before adding to extraction
  Based on DAG review, five conditions are associated with both BS (exposure) and
  dementia (outcome) and are not well captured by the modified NMI (48-predictor).
  These would be added as binary flags (5-year lookback, A+B+G diagtypes) in
  02_extract_outcomes_covariates.R and entered as covariates in Model 3.
  Candidate flags:
    sleep_apnoea    G47.3   — not in NMI; highly prevalent in BS patients; cognitive decline RF
    af_flag         I48     — not in NMI; AF is a strong dementia RF (embolic stroke)
    ihd_flag        I20-I25 — not in NMI; atherosclerosis shares pathway with vascular dementia
    depression_flag F32-F34 — NMI has rx_N06A proxy only; ICD dx more specific
    hypertension_flag I10-I15 — NMI has rx proxies only; strongest modifiable dementia RF
  Action:   Discuss with supervisors. Confirm which conditions to add and whether
            to enter as individual binary flags alongside nmi_score or replace nmi_score.
            Do NOT add these to 02 or 04 until decision is made.
  See:      study1_methods_plan.md section 5 (Covariates), section 11.

[SUPERVISOR-2] Cox model clustering — cluster() vs strata() — confirm before coding
  Current plan in methods plan is TBD (section 11 [B1]).
  Options:
    cluster(matched_pnr) — robust sandwich SE; corrects SEs only, point estimates unchanged
    strata(matched_pnr)  — stratified baseline hazard; can change point estimates
  Must be pre-specified before running the analysis.
  Action:   Confirm with supervisors and co-authors. Check what prior published
            analyses from this group / this data environment have used.
  See:      study1_methods_plan.md section 11 [B1].

================================================================================
ANALYSIS DECISIONS — CONFIRM BEFORE WRITING 07_analysis_dementia.R
================================================================================

[ANALYSIS-1] Time-period analysis method — confirm approach before coding
  The methods plan (section 7e, [B2]) pre-specifies landmark analyses at 1 year
  and 5 years but leaves the implementation approach as TBD. Three options:

  Option A — Time-band IRs and HRs (RECOMMENDED based on literature review)
    Split follow-up into bands: [0–1 yr], [1–5 yr], [5+ yr].
    Compute separate crude IR and adjusted Cox HR for each band.
    Everyone contributes to all bands they were at risk for.
    Most common approach in register-based bariatric surgery outcome studies.
    Directly addresses: early surveillance bias (0–1 yr) vs. sustained effect (5+).
    Aligns with sensitivity analysis 7g.2 (exclusion of first-12-month events).

  Option B — Landmark restriction (more formal)
    At 1-year landmark: exclude all persons who had the outcome or were censored
    before 1 year. Restart analysis from that point in the remaining cohort.
    Changes the study population at each landmark — harder to compare across
    landmarks. Less common in BS/dementia literature.

  Option C — Cumulative incidence at fixed time points (1 yr, 5 yr)
    Report the cumulative probability of dementia by 1 year and 5 years
    (Aalen-Johansen estimator, accounting for competing mortality).
    Descriptive; does not produce HRs. Complement to Kaplan-Meier curves.
    Already partially covered by Figure 2 (CIF plots).

  Current plan in 07_analysis_dementia.R: Option A (time-band analysis).
  Action:   Confirm Option A with supervisors before coding.
            If Option B is preferred, note it modifies the study population
            and estimates must be interpreted as conditional on early survival.
  See:      study1_methods_plan.md section 11 [B2].

[ANALYSIS-2] Pre-op BMI / DBSO weight distribution — where to report
  Pre-op BMI distribution (by clinical category: <30, 30–35, 35–40, 40–50, ≥50)
  and surgery-day weight are available for the BS cohort only (from DBSO).
  These are descriptive characteristics of the BS cohort.
  Action:   Decide whether to include as additional rows in Table 1 (BS columns
            only, with a note that comparators have no BMI data) or as a
            separate supplementary table describing the BS cohort.
            Currently not in methods plan — confirm with co-authors.
  Code:     bmi_preop, weight_preop, height_preop in study1_clean.rds (BS only).


================================================================================
MINOR — REFINEMENTS AND DECISIONS TO MAKE
================================================================================

[MINOR-1] Income quintiles — verify cell counts after first run
  Implementation: 3-year average income from FAIK, population quintile cutpoints
  stratified by sex × 5-year age group × year (SEPLINE approach). Implemented in
  R/03_extract_ses.R section 3.2.
  Action:   After running pipeline, verify that pop_quintile_cutpoints contains
            reasonable cell counts. If any strata have n < 100, consider widening
            to 10-year age groups for those cells.
  Reference: Hjorth et al. Clin Epidemiol 2025;17:593–624, Table 9.

[MINOR-3] OSDC diabetes file covers up to 2022 only
  Patients with diabetes onset in 2023–2024 will be classified as No_diabetes.
  Impact is minimal (very recent onset relative to surgery date).
  Action:   Check whether an updated OSDC file exists (contact SDS or OSDC team).
  Code:     R/02_extract_outcomes_covariates.R — extract_diabetes_classification().
  See:      study1_methods_plan.md section 10 (limitations).

[MINOR-4] Modified NMI — document in manuscript methods
  For Study 1, nmi_score excludes dx_F00_G30 (weight 9) and rx_N06D (weight 11)
  to avoid circular confounding with the dementia outcome. The resulting score
  uses 48 of 50 NMI predictors and has not been externally validated.
  Action:   Ensure this is clearly described in the statistical methods section
            of the manuscript. Already documented in study1_methods_plan.md
            section 5 (Covariates) and section 10 (Limitations).
  Code:     R/02_extract_outcomes_covariates.R — extract_nmi(exclude_dementia_predictors = TRUE).
