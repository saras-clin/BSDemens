================================================================================
TODO — BS & DEMENTIA PROJECT
================================================================================
Last updated: 2026-05-13

Items marked [CONFIRM] are needed before the code will run correctly.
Items marked [MINOR] are refinements and decisions that can wait.

All CRITICALs have been resolved. Pipeline is ready for first run.


================================================================================
CONFIRM — VERIFY BEFORE TRUSTING RESULTS
================================================================================

[CONFIRM-3] OSDC file — diabetes_type values not yet verified
  Path: E:/workdata/708421/cleaned-data/diabetes_register_pop/dm_population_1977_2022.rds
  Confirmed columns: PNR, diabetes_type, do_dm, sex, date_of_birth, age_at_onset
  Action:   Run table(dm$diabetes_type) to confirm code values.
            Code assumes "T1D" / "T2D" string labels — verify this is correct.
            Also check whether an updated file (covering 2023-2024) is available.

[CONFIRM-8] aar column name in UDDA, FAIK, AKM
  BEF confirmed to use aar. UDDA, FAIK, AKM all assumed to use aar.
  Action:   Run colnames() on each register and confirm.
            load_database("udda") %>% rename_with(tolower) %>% colnames()
            load_database("faik") %>% rename_with(tolower) %>% colnames()
            load_database("akm")  %>% rename_with(tolower) %>% colnames()


================================================================================
MINOR — REFINEMENTS AND DECISIONS TO MAKE
================================================================================

[MINOR-1] Income quintiles: within-cohort vs population reference
  Current: Income quintile calculated within the study cohort (pragmatic).
  SEPLINE recommends: age/sex-standardised quintiles from the general population.
  Action:   Decide whether to use population reference quintiles.
            If yes: requires pulling FAIK for all of Denmark, not just cohort.
  Code:     extract_ses.R — ntile() in income section.

[MINOR-2] OSDC covers only to 2022 — patients with surgery 2023-2024
  Impact:   Patients who developed diabetes after 2022 and had surgery in
            2023-2024 will be classified as "No_diabetes".
  Action:   Check whether an updated OSDC file exists (contact SDS or OSDC team).
  Code:     extract_outcomes_covariates.R — extract_diabetes_classification().

[MINOR-3] Matching ratios — confirm with supervisor
  Current:  1:25 GP, 1:5 obesity, exact matching on sex + birth year (±1).
  Action:   Confirm ratios with supervisor. Consider whether index date matching
            (same calendar month) should be added.
  Code:     build_cohorts.R — N_GP_PER_BS, N_OBESITY_PER_BS constants.

[MINOR-7] UDDA — verify whether it is annual status or event-based
  If UDDA is event-based (records only when education was completed), then
  filter(aar == index_year) may miss people whose most recent education event
  was before the index year.
  Action:   Confirm with data manager or check TIMES documentation for UDDA.

[MINOR-11] senare_afkraeftet filter — verify NAs before changing
  Current:  is.na(senare_afkraeftet) | senare_afkraeftet != "Ja"  (keeps NAs — defensive)
  OSDC:     senare_afkraeftet == "Nej"  (drops NAs — only if NAs never occur)
  Action:   After first run, check: table(is.na(lpr_a_diagnose$senare_afkraeftet))
            If no NAs exist, switch to == "Nej" for consistency with OSDC.
            If NAs exist, current filter is correct (includes unconfirmed retraction status).
  Code:     get_lpr_diagnoses() in 02; get_prior_dementia_pnrs() and E66 filter in 01.

[MINOR-10] Check if dstDataPrep has prepare_lpr2() / prepare_lpr3f() helpers
  If available, could replace manual join-and-rename logic in get_lpr_diagnoses().
  Action:   Run ls("package:dstDataPrep") or ?prepare_lpr2 in a DST session.
  Code:     extract_outcomes_covariates.R — get_lpr_diagnoses()
            build_cohorts.R — get_prior_dementia_pnrs()

[MINOR-14] Learn targets pipeline package before analysis phase
  The {targets} package tracks dependencies and only reruns outdated steps.
  Recommended for DST projects by CTP/RDST group.
  Resources: https://books.ropensci.org/targets/
             https://github.com/tagteam/registerTargets/tree/main
  Action:   Learn before starting Cox model scripts.

[MINOR-15] GLP-1 exclusion in obesity comparator — confirm with co-authors
  Persons on GLP-1 agonists (ATC A10BJ) before index date may have altered
  dementia risk. Tentative plan: include in main analysis with GLP-1 as
  covariate; add sensitivity analysis excluding.
  Action:   Confirm analytical decision with co-authors before implementing.

[MINOR-16] DBSO underreporting 2010-2012 — check match rates after first run
  Reporting may have been incomplete in early years of mandatory registration.
  Action:   After running main_build_cohorts(), examine match rate warnings.
            If zero-match rates are high for 2010-2012, add sensitivity analysis
            restricting to index dates from 2013+.


================================================================================
DBSO REFERENCE INFORMATION
================================================================================
Official name: Databasen for Behandling af Svær Overvægt (DBSO)
Operated by:   SunDK (formerly RKKP)
Coverage:      All public and private hospitals; mandatory since 2010
Parquet:       Prepared once by 00_prepare_dbso.R; access via load_database("dbso")

Column names confirmed (after rename_with(tolower)):
  pnr, datoper_prim (surgery date), gastricbypass_prim, gastricsleeve_prim,
  redo_prim, datopre, datofol, udgangsvaegtpre_prim, vaegtper_prim, vaegtfol,
  hoejde, koen ("M"/"K"), plus medkomplik* and medication columns.

Register column names confirmed:
  dodsaars:     pnr, d_dodsdto (death date), fdato, c_sex, v_alder, year, ...
  lmdb:         pnr, atc, eksd (dispensing date), indo, volume, apk, year, ...
  lpr_a_kontakt: pnr, dw_ek_kontakt, kont_starttidspunkt (contact datetime),
                kont_type (contact type; "ALCA00" = inpatient), kont_sluttidspunkt, ...
  lpr_a_diagnose: dw_ek_kontakt, diagnosekode, diagnosetype, senare_afkraeftet
  vnds:         pnr, haend_dato (event date), indud_kode ("U"=emigration, "I"=immigration),
                indud_land, fdato, year
================================================================================
